<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="csrf-token" content="{{ csrf_token() }}">
  <title>🎉 Live Exam Access - {{ config('app.name', 'Bikolpo LQ') }}</title>

  @vite(['resources/css/app.css','resources/js/app.js'])
  <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://kit.fontawesome.com/a2d04c48b5.js" crossorigin="anonymous"></script>

  <style>
    body { font-family: 'Fredoka', sans-serif; }
    .bubble {
      position: absolute; border-radius: 50%; opacity: 0.4; animation: float 10s infinite ease-in-out;
    }
    @keyframes float {
      0%,100%{ transform:translateY(0); }
      50%{ transform:translateY(-40px); }
    }
    .btn-fun {
      background: linear-gradient(90deg,#f97316,#f43f5e,#8b5cf6);
      background-size: 300% 100%;
      transition: all .4s ease;
    }
    .btn-fun:hover {
      background-position: right center;
      transform: translateY(-3px) scale(1.02);
    }
  </style>
</head>

<body class="min-h-screen bg-gradient-to-br from-pink-100 via-purple-100 to-indigo-200 relative overflow-hidden">

  <!-- Floating bubbles -->
  <div class="bubble w-36 h-36 bg-pink-300 top-10 left-10 animate-[float_8s_infinite]"></div>
  <div class="bubble w-48 h-48 bg-yellow-300 bottom-20 right-16 animate-[float_12s_infinite]"></div>
  <div class="bubble w-20 h-20 bg-indigo-300 bottom-32 left-1/3 animate-[float_10s_infinite]"></div>

  <!-- Main container -->
  <div class="flex items-center justify-center min-h-screen relative z-10 px-4">
    <div class="bg-white rounded-3xl shadow-2xl p-8 md:p-12 w-full max-w-md transform hover:scale-[1.01] transition-all duration-300">

      <!-- Header -->
      <div class="text-center mb-8">
        <div class="w-20 h-20 bg-gradient-to-tr from-indigo-400 to-pink-400 rounded-2xl mx-auto flex items-center justify-center shadow-lg">
          <i class="fa-solid fa-gamepad text-white text-3xl"></i>
        </div>
        <h1 class="text-3xl md:text-4xl font-extrabold text-gray-800 mt-5">🚀 Join Live Exam</h1>
        <p class="text-gray-500 mt-2">Fun & fast access with Bikolpo LQ</p>
      </div>

      <!-- Error -->
      @if ($errors->any())
        <div class="mb-6 p-4 bg-red-100 border-l-4 border-red-400 rounded-xl">
          <p class="text-red-700 font-semibold">Oops! Check your details 👀</p>
          <ul class="text-sm text-red-600 mt-2 list-disc list-inside">
            @foreach ($errors->all() as $error)
              <li>{{ $error }}</li>
            @endforeach
          </ul>
        </div>
      @endif

      <!-- Form -->
      <form method="POST" action="{{ route('public.quiz.process-access') }}" class="space-y-6">
        @csrf

        <!-- Phone -->
        <div>
          <label for="phone" class="block text-sm font-semibold text-gray-700 mb-1">📱 Phone Number</label>
          <input type="tel" id="phone" name="phone" value="{{ old('phone') }}"
                 placeholder="017xxxxxxxx"
                 pattern="01[3-9][0-9]{8}"
                 maxlength="11"
                 required
                 class="w-full px-4 py-3 rounded-2xl border border-gray-300 focus:ring-2 focus:ring-pink-400 focus:border-pink-400 text-gray-800 text-base shadow-sm">
        </div>

        <!-- Access code -->
        <div>
          <label for="access_code" class="block text-sm font-semibold text-gray-700 mb-1">🔑 Access Code</label>
          <input type="text" id="access_code" name="access_code" value="{{ old('access_code') }}"
                 placeholder="6-digit code"
                 maxlength="6"
                 pattern="[0-9]{6}"
                 required
                 class="w-full px-4 py-3 rounded-2xl border border-gray-300 focus:ring-2 focus:ring-indigo-400 focus:border-indigo-400 text-gray-800 text-center tracking-widest text-lg shadow-sm">
        </div>

        <!-- Submit -->
        <button type="submit"
                class="btn-fun w-full py-3 rounded-2xl text-white font-bold text-lg shadow-lg flex items-center justify-center gap-2">
          <i class="fa-solid fa-play"></i> Start Live Exam
        </button>
      </form>

      <!-- Extra -->
      <div class="text-center mt-6">
        <button type="button"
                onclick="handleMultipleExams()"
                class="px-5 py-2 bg-indigo-50 hover:bg-indigo-100 text-indigo-700 rounded-xl border border-indigo-200 transition text-sm font-medium">
          <i class="fa-solid fa-list-ul mr-1"></i> See All Exams
        </button>
      </div>

    </div>
  </div>

  <script>
    function handleMultipleExams(){
      alert("Showing multiple exams (demo) 🎯");
    }
  </script>
</body>
</html>
